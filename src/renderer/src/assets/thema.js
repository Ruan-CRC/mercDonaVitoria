import { createTheme } from '@mui/material'

export const appThema = createTheme({
  palette: {
    primary: {
      main: '#012a4a'
    },
    secondary: {
      main: '#2a6f97'
    },
    text: {
      main: '#ddf7ff'
    }
  }
})
